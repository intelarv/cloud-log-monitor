# Per-run eval outputs are regenerated by `pnpm run eval`; only baseline.json
# is versioned (it's the regression anchor the gate compares against).
results/

# Rolling per-run score history the nightly notifier appends to (trend
# indicator). Run output, not a versioned anchor; persist it on a durable path
# in an ephemeral CI/cron pod so the trend survives across nightly runs.
score-history.json
